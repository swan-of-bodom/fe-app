import { ReactComponent as ThreeDots } from "./icon.svg";

export const LinksMenu = () => {
  return (
    <div style={{ display: "flex", marginLeft: "1em", cursor: "pointer" }}>
      <ThreeDots />
    </div>
  );
};
