import { createTheme } from "@mui/material";

export const baseTheme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#ffb000",
    },
    background: {
      default: "#070718",
      paper: "#020210",
    },
  },
});
